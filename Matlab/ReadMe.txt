Written in Matlab R2017a.

BEFORE RUNNING (only do once): In 'libsvm_matlab/' run 'make.m'. If you receive an error, type 'mex -setup' (in Matlab command window) then select c++ compiler. Now rerun 'make.m'.

SVM_example.pdf provides an example of the expected results for all four sum kernels.
